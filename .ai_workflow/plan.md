# Log Validation Plan

> Generated by the `validate-logs` skill.
> Consumed by the `fix-log-issues` skill.

**Generated:** 2026-03-26T00:48:00.000Z
**Source runs:** workflow_20260325_131222, workflow_20260325_132803, workflow_20260325_162055

---

## Issues

### RI-001 — Undocumented scripts/colors.sh

- **ID:** RI-001
- **Source step:** step_03
- **Type:** undocumented-directory
- **Priority:** Low
- **Path:** scripts/colors.sh
- **Description:** `scripts/colors.sh` exists as a shared ANSI colour helper for shell scripts but is not referenced in any project documentation (README.md, ARCHITECTURE.md, CONTRIBUTING.md, or ROADMAP.md).
- **Fix:** Add a one-line entry for `scripts/colors.sh` in `docs/ARCHITECTURE.md` under the `scripts/` row of the directory table, describing it as the shared ANSI colour definitions helper sourced by other shell scripts.
- **Status:** done

---

### RI-002 — Duplicated createObserver() test helper

- **ID:** RI-002
- **Source step:** step_06
- **Type:** architecture-mismatch
- **Priority:** Medium
- **Path:** test/
- **Description:** `createObserver()` is independently defined in `test/DualObserverSubject.test.ts` (line 12), `test/ObserverMixin.test.ts` (line 14), and `test/integration.test.ts` (line 193); no shared test-helper module exists, causing duplication.
- **Fix:** Create `test/helpers.ts` exporting `createObserver()`, then replace the three local definitions with imports from `test/helpers.ts`.
- **Status:** done

---

### RI-003 — version-sync node -e command fails with SyntaxError

- **ID:** RI-003
- **Source step:** step_16
- **Type:** architecture-mismatch
- **Priority:** Medium
- **Path:** .workflow-config.yaml
- **Description:** The `version_sync.command` and `version_sync.check_command` fields in `.workflow-config.yaml` wrap their JavaScript in outer `"…"` double-quotes; the workflow runner invokes them in a way that causes Node.js to receive `"` as the first character of the script, resulting in `SyntaxError: Unterminated string constant` on every run.
- **Fix:** Extract the inline JavaScript from both `command` and `check_command` into a dedicated `scripts/version-sync.js` file, then update both fields in `.workflow-config.yaml` to call `node scripts/version-sync.js` (with optional `--check` flag) instead of the inline `node -e "…"`.
- **Status:** done
